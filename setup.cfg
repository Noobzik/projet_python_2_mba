[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    build,
    dist,
    .eggs,
    *.egg-info

[tool:pytest]
testpaths = tests
addopts = --cov=banking_api --cov-report=term-missing --cov-report=xml
